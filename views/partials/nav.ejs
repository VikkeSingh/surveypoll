<nav class="navbar navbar-dark bg-dark px-4">
    <a class="navbar-brand mb-0 text-decoration-none" href="/">Surveypoll</a>
    <div class="dropdown">
        <a class="text-white dropdown-toggle text-decoration-none fw-semibold" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= String(user).toUpperCase() %>
        </a>
        <ul class="dropdown-menu dropdown-menu-end">
            <li>
                <a class="dropdown-item<%= active === 'dashboard' ? ' active' : '' %>" href="/">
                    <i class="bi bi-speedometer2 me-2"></i>Dashboard
                </a>
            </li>
            <li>
                <a class="dropdown-item<%= active === 'users' ? ' active' : '' %>" href="/admin/users">
                    <i class="bi bi-person-plus me-2"></i>Add User
                </a>
            </li>
            <li>
                <a class="dropdown-item<%= active === 'password' ? ' active' : '' %>" href="/admin/password">
                    <i class="bi bi-key me-2"></i>Change Password
                </a>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
                <form method="post" action="/logout">
                    <button type="submit" class="dropdown-item">
                        <i class="bi bi-box-arrow-right me-2"></i>Logout
                    </button>
                </form>
            </li>
        </ul>
    </div>
</nav>
