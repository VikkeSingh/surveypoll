services:
  mongo:
    image: mongo:7
    container_name: surveypoll-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - surveypoll-mongo-data:/data/db

volumes:
  surveypoll-mongo-data:
