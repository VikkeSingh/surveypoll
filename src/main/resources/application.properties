server.port=${PORT:8080}

# MongoDB connection (set MONGODB_URI in the hosting platform; local default below)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://dbuser:dbUserPassword@cluster0.kktk8ps.mongodb.net/surveypoll?appName=Cluster0}

# Dashboard login credentials (ov
#
# erride via env vars in production)
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:admin123}

# Jackson date formatting
spring.jackson.time-zone=UTC

# Static resources
spring.web.resources.static-locations=classpath:/static/
